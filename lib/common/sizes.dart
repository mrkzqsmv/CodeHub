import 'package:flutter/material.dart';

const scaffoldPadding = EdgeInsets.all(15);

//VERTICAL PADDING
const verticalPadding10 = EdgeInsets.symmetric(vertical: 10);
const verticalPadding5 = EdgeInsets.symmetric(vertical: 5);

//HORIZONTAL PADDING
const horizontalPadding10 = EdgeInsets.symmetric(horizontal: 10);
const horizontalPadding5 = EdgeInsets.symmetric(horizontal: 5);

//ALL PADDING
const right10 = EdgeInsets.only(right: 10);
const right5 = EdgeInsets.only(right: 5);
const all5 = EdgeInsets.all(5);
const all10 = EdgeInsets.all(10);
const all15 = EdgeInsets.all(15);
const top10 = EdgeInsets.only(top: 10);
